import { Queue } from './queue';

// You can find the problem descriptions in ./README.md
describe('Queue', () => {
  describe('Problem 1', () => {
    test('Solution 1', () => {
      expect(true).toBe(true);
    });
  });
});
